{
  "entities": {
    "AppState": {
      "title": "AppState",
      "description": "Consolidated application state for Kitutu Chache TVC system",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "description": "JSON object storing full application entities"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when state was last saved"
        }
      },
      "required": ["data", "updatedAt"]
    }
  },
  "firestore": {
    "/app_state/timetable_state": {
      "schema": "AppState",
      "description": "Master document storing application timetable and ERP state"
    },
    "/app_state/timetable": {
      "schema": "AppState",
      "description": "Modular document storing active timetable entries, units, and course groups"
    },
    "/app_state/website": {
      "schema": "AppState",
      "description": "Modular document storing dynamic website CMS configuration"
    }
  }
}
